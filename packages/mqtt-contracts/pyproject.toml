[project]
name = "jaljasoos-mqtt-contracts"
version = "0.1.0"
description = "MQTT Schemas and Validators for JalJasoos"
requires-python = ">=3.10"
dependencies = [
    "jsonschema>=4.21.0"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
